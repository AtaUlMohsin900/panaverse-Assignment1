/*Pizzas: Think of at least three kinds of your favorite pizza. Store these pizza names in a array,
 and then use a for loop to print the name of each pizza.*/
var pizza = ['Cheese Pizza', 'Vegetable Pizza', 'BBQ Pizza'];
// Price
for (var i = 0; i < pizza.length; i++) {
    console.warn(pizza[i]);
}
// Message
for (var i = 0; i < pizza.length; i++) {
    console.warn("I like", pizza[i]);
}
console.warn('I like pizza');
